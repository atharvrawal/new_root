# Runtime dependencies

# UI toolkit for the response window - native rich-text/Markdown
# rendering via QTextBrowser (setMarkdown()), no browser engine.
PySide6>=6.7

# Direct Gemini API access, replacing the old webview/adapter layer.
google-genai>=0.3.0

# Screenshot capture for the queue-and-send feature. Pure ctypes on
# Windows, no Pillow needed - see capture.py.
mss>=9.0.1

# --- Build tooling (not required at runtime) --------------------------
# Install separately, or via: pip install -r requirements-build.txt
